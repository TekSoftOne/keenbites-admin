import React, { FC } from 'react';
import { ExpiredRequestComponent } from '../expired-requests/expired-requests-component';

export const ExpiredRequestPage: FC = () => {
    return <ExpiredRequestComponent />;
};
